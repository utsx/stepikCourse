#include <bits/stdc++.h>

typedef long long ll;

using namespace std;

int main()
{
    double x, y;
    cin >> x >> y;
    if(abs(x) + abs(y) <=  1)
    {
        cout << "YES";
    }
    else
    {
        cout << "NO";
    }
}
